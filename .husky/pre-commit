#!/bin/sh
set -eu

if [ "${HUSKY_SKIP:-0}" = "1" ] || [ "${HUSKY_SKIP_PRECOMMIT:-0}" = "1" ]; then
    echo "Skipping pre-commit checks (HUSKY_SKIP/HUSKY_SKIP_PRECOMMIT)."
    exit 0
fi

echo "Running staged pre-commit checks..."
npm run precommit
