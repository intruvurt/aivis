const config = {
  appId: 'biz.aivis.app',
  appName: 'AiVIS ~ AI Visibility Intelligence Audits',
  webDir: 'dist',
  bundledWebRuntime: false,
  server: {
    androidScheme: 'https',
    iosScheme: 'https'

  }
};

export default config;
