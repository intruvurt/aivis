# Railway.toml - Production deployment configuration
# This file defines how Railway builds and deploys the monorepo services

[build]
builder = "nixpacks"

[deploy]
# Global restart policy for reliability
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
