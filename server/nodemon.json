{
  "watch": ["src/**/*.ts", "../shared/**/*.ts"],
  "ext": "ts,json,env",
  "ignore": ["node_modules", "dist", "src/**/*.test.ts", "src/**/*.spec.ts"],
  "exec": "tsx src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
